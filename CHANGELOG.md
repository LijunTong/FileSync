# 更新日志

## v1.1.0 - 2026-05-06

### ✨ 新增功能

#### 1. 任务进度条
- 在任务列表中新增「进度」列，实时显示同步进度
- 进度条样式优化，配合浅色/深色主题
- 运行时进度平滑更新，结束后自动归零

#### 2. 下次运行时间显示
- 在任务列表中新增「下次运行」列
- 每秒自动更新下次执行时间
- 禁用的任务不显示下次运行时间
- 手动任务显示空白

#### 3. 编辑页面显示下5次执行
- 在任务编辑页面预览接下来5次运行时间
- 帮助用户验证调度设置是否正确

#### 4. 界面优化
- 标题从 "FileSync" 改为中文 "文件同步"
- 标题栏左侧添加应用图标
- 状态栏右下角显示版本号

#### 5. 交互改进
- 工具栏移除「立即执行」和「日志」按钮，界面更简洁
- 任务列表的「双向」和「启用」改为只读显示
- 右键菜单新增「启用/禁用任务」功能
- 右键菜单保留「立即执行」和「查看日志」

#### 6. 过滤功能修复
- 修复文件过滤不起作用的问题
- 优化多模式过滤（*.jpg;*.png）
- 支持带空格的过滤表达式
- 使用正则表达式实现精确匹配

### 📝 用户体验改进

#### 1. 列顺序优化
- 调度 → 状态 → 进度 → 上次运行 → 下次运行 → 操作
- 时间相关的列排列在一起，更直观

#### 2. 视觉优化
- 进度条使用主题色，颜色更醒目
- 状态图标更清晰
- 整体布局更协调

### 🛠️ 技术改进

#### 1. SyncTask 模型增强
- 新增 `IsRunning`、`ProgressValue`、`ProgressMax` 属性
- 完整实现 `INotifyPropertyChanged` 接口
- `IsEnabled` 变更时自动触发 UI 更新

#### 2. MainViewModel 改进
- 添加定时更新机制（每秒刷新下次运行时间）
- 新增 `VersionNumber` 属性获取程序集版本

#### 3. SyncEngine 优化
- 同步开始时设置运行状态
- 每次处理文件时更新进度
- 同步结束后自动重置进度
- 为测试添加 100ms 延迟（可移除）

### 📦 版本信息
- AssemblyVersion: 1.1.0.0
- AssemblyFileVersion: 1.1.0.0

---

## v1.0.0 - 2026-05-06

### ✨ 初始版本功能

#### 核心功能
- 多任务管理
- 双向同步、增量更新、备份模式
- 灵活的调度系统（手动/分钟/小时/天/周/Cron）
- 文件冲突检测
- 完整的运行日志
- 深色/浅色主题切换
- 系统托盘支持
- 开机自启动

#### 技术特性
- WPF + MVVM 架构
- SQLite 数据存储
- 现代化 UI 设计
