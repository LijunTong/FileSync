<Window x:Class="FileSync.Views.ConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认" Height="200" Width="420"
        MinHeight="200" MinWidth="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border x:Name="MainBorder"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundSecondaryBrush}"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="16,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Style="{StaticResource IconPath}"
                              Data="{DynamicResource QuestionIcon}"
                              Stroke="{DynamicResource PrimaryBrush}"
                              Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TitleText" FontSize="13" FontWeight="600"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="确认"/>
                    </StackPanel>

                    <Button Grid.Column="1" Style="{StaticResource TitleBarCloseButton}" Click="CloseButton_Click" Width="40" Height="32">
                        <Path Style="{StaticResource IconPath}"
                              Data="{DynamicResource CloseIcon}"
                              Stroke="{DynamicResource TextSecondaryBrush}"
                              Width="12" Height="12" Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Border>

            <!-- 内容区 -->
            <Grid Grid.Row="1" Margin="20,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           x:Name="MessageText"
                           FontSize="14" 
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button x:Name="CancelButton" Style="{StaticResource OutlineButton}" Content="取消" Width="80"
                            Click="CancelButton_Click" Margin="0,0,8,0"/>
                    <Button x:Name="ConfirmButton" Style="{StaticResource PrimaryButton}" Content="确定" Width="80"
                            Click="ConfirmButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
