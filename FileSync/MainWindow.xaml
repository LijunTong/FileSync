<Window x:Class="FileSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:FileSync.Controls"
        Title="FileSync" Height="680" Width="1020"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize">

    <Border x:Name="MainBorder"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource BackgroundSecondaryBrush}"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="FileSync" FontSize="13" FontWeight="600"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox Content="开机自启" IsChecked="{Binding AutoStart}" Margin="0,0,12,0"
                                  Style="{StaticResource ModernCheckBox}" FontSize="12"/>
                        <Button x:Name="ThemeToggleButton" Style="{StaticResource IconButton}"
                                Click="ThemeToggle_Click" ToolTip="切换主题" Width="32" Height="32">
                            <Path x:Name="ThemeIcon" Style="{StaticResource IconPath}"
                                  Data="{DynamicResource MoonIcon}"
                                  Stroke="{DynamicResource TextSecondaryBrush}"
                                  Width="14" Height="14" Stretch="Uniform"/>
                        </Button>
                        <Button Style="{StaticResource TitleBarButton}" Click="Minimize_Click" Width="36" Height="32">
                            <Path Style="{StaticResource IconPath}"
                                  Data="{DynamicResource MinimizeIcon}"
                                  Stroke="{DynamicResource TextSecondaryBrush}"
                                  Width="12" Height="12" Stretch="Uniform"/>
                        </Button>
                        <Button x:Name="MaximizeButton" Style="{StaticResource TitleBarButton}"
                                Click="Maximize_Click" Width="36" Height="32">
                            <Path Style="{StaticResource IconPath}"
                                  Data="{DynamicResource MaximizeIcon}"
                                  Stroke="{DynamicResource TextSecondaryBrush}"
                                  Width="11" Height="11" Stretch="Uniform"/>
                        </Button>
                        <Button Style="{StaticResource TitleBarCloseButton}" Click="Close_Click" Width="40" Height="32">
                            <Path Style="{StaticResource IconPath}"
                                  Data="{DynamicResource CloseIcon}"
                                  Stroke="{DynamicResource TextSecondaryBrush}"
                                  Width="12" Height="12" Stretch="Uniform"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 工具栏 -->
            <Border Grid.Row="1" Background="{DynamicResource BackgroundSecondaryBrush}"
                    Padding="16,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{Binding Tasks.Count, StringFormat='{}{0} 个同步任务'}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="13" VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Style="{StaticResource PrimaryButton}" Command="{Binding AddTaskCommand}">
                            <StackPanel Orientation="Horizontal">
                                <Path Style="{StaticResource IconPath}"
                                      Data="{DynamicResource PlusIcon}"
                                      Stroke="{DynamicResource PrimaryButtonForegroundBrush}"
                                      Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="添加任务"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource OutlineButton}" Command="{Binding ExecuteTaskCommand}"
                                Margin="8,0,0,0" IsEnabled="{Binding IsNotBusy}">
                            <StackPanel Orientation="Horizontal">
                                <Path Style="{StaticResource IconPath}"
                                      Data="{DynamicResource PlayIcon}"
                                      Stroke="{DynamicResource TextPrimaryBrush}"
                                      Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="立即执行"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource GhostButton}" Command="{Binding ViewLogsCommand}"
                                Margin="4,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <Path Style="{StaticResource IconPath}"
                                      Data="{DynamicResource ClipboardIcon}"
                                      Stroke="{DynamicResource TextSecondaryBrush}"
                                      Width="14" Height="14" Stretch="Uniform" Margin="0,0,6,0"/>
                                <TextBlock Text="日志"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 主内容区 -->
            <Border Grid.Row="2" Padding="16,12">
                <Grid>
                    <!-- 自定义右键弹出菜单 -->
                    <controls:CustomPopupMenu x:Name="TaskContextMenu"/>

                    <!-- 任务列表 -->
                    <Border Style="{StaticResource ModernCard}" Padding="0"
                            Visibility="{Binding HasTasks, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <DataGrid ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}"
                                  Style="{StaticResource ModernDataGrid}"
                                  AlternationCount="2"
                                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                                  MouseRightButtonUp="DataGrid_MouseRightButtonUp">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="140">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="500"/>
                                            <Setter Property="FontSize" Value="14"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="Padding" Value="12,0"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="源目录" Binding="{Binding SourcePath}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontSize" Value="13"/>
                                            <Setter Property="Padding" Value="8,0"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="ToolTip" Value="{Binding SourcePath}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="目标目录" Binding="{Binding TargetPath}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontSize" Value="13"/>
                                            <Setter Property="Padding" Value="8,0"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="ToolTip" Value="{Binding TargetPath}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridCheckBoxColumn Header="双向" Binding="{Binding IsBidirectional}" Width="60">
                                    <DataGridCheckBoxColumn.ElementStyle>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridCheckBoxColumn.ElementStyle>
                                </DataGridCheckBoxColumn>
                                <DataGridTemplateColumn Header="启用" Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Click="EnableCheckBox_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="调度" Binding="{Binding ScheduleDisplay}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontSize" Value="13"/>
                                            <Setter Property="Padding" Value="8,0"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="状态" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Text="{Binding LastStatus, Converter={StaticResource StatusIconConverter}}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="上次运行" Binding="{Binding LastRunTime, StringFormat=MM-dd HH:mm}" Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontSize" Value="12"/>
                                            <Setter Property="Padding" Value="8,0"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextTertiaryBrush}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Header="操作" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Style="{StaticResource RowActionButton}" Click="RowEdit_Click" ToolTip="编辑">
                                                    <Path Style="{StaticResource IconPath}"
                                                          Data="{DynamicResource EditIcon}"
                                                          Stroke="{DynamicResource TextSecondaryBrush}"
                                                          Width="15" Height="15" Stretch="Uniform"/>
                                                </Button>
                                                <Button Style="{StaticResource RowActionButton}" Click="RowDelete_Click" ToolTip="删除">
                                                    <Path Style="{StaticResource IconPath}"
                                                          Data="{DynamicResource TrashIcon}"
                                                          Stroke="{DynamicResource DangerBrush}"
                                                          Width="15" Height="15" Stretch="Uniform"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- 空状态 -->
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="{Binding HasTasks, Converter={StaticResource InverseBooleanConverter}}">
                        <Path Style="{StaticResource IconPath}"
                              Data="{DynamicResource FolderIcon}"
                              Stroke="{DynamicResource TextTertiaryBrush}"
                              Width="48" Height="48" Stretch="Uniform" Opacity="0.5"
                              HorizontalAlignment="Center" Margin="0,0,0,16"/>
                        <TextBlock Text="还没有同步任务" FontSize="16" FontWeight="500"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="点击上方「添加任务」创建第一个备份任务"
                                   FontSize="13" Foreground="{DynamicResource TextTertiaryBrush}"
                                   HorizontalAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 状态栏 -->
            <Border Grid.Row="3" Background="{DynamicResource BackgroundSecondaryBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,8,8">
                <Grid Margin="16,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>

                    <ProgressBar Grid.Column="1" Style="{StaticResource ModernProgressBar}"
                                 Value="{Binding ProgressValue}" Maximum="{Binding ProgressMaximum}"
                                 Width="200" HorizontalAlignment="Right"
                                 Visibility="{Binding IsProgressVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>